

object operators {
  def main(args: Array[String]): Unit = {
    var y: Int = 5
    println("1 x " + y + " = " + (1 * y))
    println("10 x " + y + " = " + (10 * y))
    println("100 x " + y + " = " + (100 * y))
  }
}